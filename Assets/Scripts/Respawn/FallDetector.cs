using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class FallDetector : MonoBehaviour
{

    /*static FallDetector instance;
    static bool hasHit = false;

    void Start ()
    {
        if (instance == null)
        {
            instance = this;
        }
    }

    //check if the player has entered the fall detector
    void OnTriggerEnter2D(Collider2D other)
    {
        //if the player hits the fall detector
        if (other.CompareTag("Player"))
        {
            //the fall detector has been hit.
            //hasHit = true;

            //reload scene
            SceneManager.LoadScene(SceneManager.GetActiveScene().buildIndex);

        }        
    }

    public static bool returnBoolean()
    {
        //if the fall detector has been hit, this will be true
        return hasHit;
        //whatever hasHit was before, not just make it false
        hasHit = false;

    }*/

}

